const EmailConstants = {
    affiliateName: '$(affiliate.name)',
    competitionName: '$(competition.name)',
    competitionDescription: '$(competition.description)',
    competionStartDate: '$(competition.date.start)',
    userFirstName: '$(user.firstName)',
    userLastName: '$(user.lastName)',
    regoOpenDate: '$(rego.form.date.open)',
    regoCloseDate: '$(rego.form.date.close)',
    regoTrainingDays: '$(rego.form.training.days)',
    fromContactEmail: '$(contact.email)',
    fromContactEmailLink: '$(contact.email.link)',
    fromContactName: '$(contact.name)',
    fromContactMobile: '$(contact.mobile)',
    fromContactMobileLink: '$(contact.mobile.link)',
    registerLink: '$(register.link)',
    registrants: '$(registration.registrants)',
    contacts: '$(competition.contacts)',
    credentials: '$(user.credentials)',
    creatorName: '$(creator.name)',
    registrantNam: '$(registrant.name)',
    profileLink: '$(profile.link)',
    otherLogins: '$(otherLogins)',
    otherLoginsToReplace: '$(otherLogins)<br/><br/>',
    email: '$(email)',
    teamName: '$(team.name)'
};

export default EmailConstants;